Add badge to a dashboard
---
security:
  - BearerAuth: []
tags:
  - 'dashboard'
parameters:
  - $ref: '#/components/parameters/AuthorizationHeaderParameter'
  - $ref: '#/components/parameters/ResourceIdParameter'
  - $ref: '#/components/parameters/BadgeNameParameter'
  - $ref: '#/components/parameters/BadgeCategoryParameter'
responses:
  401:
    description: 'Auth Token Missing, Invalid or Expired'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'
  200:
    description: 'The badge was added successfully'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/MessageResponse'
  404:
    description: 'Dashboard not found, or badge is not whitelisted'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'
