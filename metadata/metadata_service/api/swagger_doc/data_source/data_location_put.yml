Create or update a Data Location
Creates a new data location or updates an existing one
---
security:
  - BearerAuth: []
tags:
  - 'data_source'
parameters:
  - $ref: '#/components/parameters/AuthorizationHeaderParameter'
requestBody:
  required: true
  content:
    application/json:
      schema:
        allOf:
          - $ref: '#/components/schemas/DataLocationFields'
          - $ref: '#/components/schemas/Publisher'
responses:
  400:
    $ref: '#/components/responses/ValidationErrorResponse'
  401:
    $ref: '#/components/responses/AuthTokenInvalidResponse'
  403:
    $ref: '#/components/responses/ReadOnlyModeResponse'
  200:
    description: 'Data location updated'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/KeyStatus'
  201:
    description: 'Data location created'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/KeyStatus'
  404:
    description: 'Failed to create/update data location'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'
