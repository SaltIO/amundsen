Update the user owner information
---
security:
  - BearerAuth: []
tags:
  - 'user'
parameters:
  - $ref: '#/components/parameters/AuthorizationHeaderParameter'
  - name: user_id
    in: path
    example: 'roald9@example.org'
    type: string
    schema:
      type: string
    required: true
  - name: resource_type
    in: path
    example: 'table'
    description: 'resource_type is ignored at the moment'
    type: string
    schema:
      type: string
    required: true
  - $ref: '#/components/parameters/TableUriParameter'
requestBody:
  $ref: '#/components/requestBodies/PublisherRequest'
responses:
  401:
    $ref: '#/components/responses/AuthTokenInvalidResponse'
  403:
    $ref: '#/components/responses/ReadOnlyModeResponse'
  200:
    description: 'User was added as owner successfully'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/MessageResponse'
  500:
    description: 'Internal server error'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ErrorResponse'
